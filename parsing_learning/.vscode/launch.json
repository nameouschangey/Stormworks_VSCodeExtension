{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Lua",
            "type": "lua",
            "request": "launch",
            "program": "${file}"
        }
    ]
}